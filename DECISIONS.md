# Decisions

Architecture and scope decisions, in the order they were made. Two lines each: what, why.

## Checkpoint 1

**Contract sharing pattern**: oRPC router is defined directly in `packages/contracts` (procedures + handlers together), not split into a separate contract-first layer. `apps/web` imports the router's type only (`import type`), so server code never reaches the frontend bundle — simplest path to full type safety, mirrors tRPC's ergonomics.

**Docker Compose scope**: only Postgres runs in Docker locally; `apps/server` and `apps/web` run natively via `bun run dev`. Matches how Railway will run them as three separate native services, so no Dockerfiles are needed until deploy.

**Typecheck strategy**: per-package `tsconfig.json`s use TS project references (`composite: true`), so a single root `tsc -b` incrementally typechecks every workspace and catches cross-package breaks.

**`TrainingPlan.config` / `PlannedWorkout.prescription` as `Json`, not `String`**: keeps the schema flexible while the plan-generation module's exact shape is still being designed (Checkpoint 3+), avoiding a migration when it settles.

**`PlannedWorkout.day` and `*.type` fields as Prisma enums, not free strings**: compile-time safety for the plan-generation logic. Actual calendar date is derived from `startDate + week + day`, never stored directly.

**Unit test runner: `bun test`, no Vitest**: the stack is already Bun-native; avoids an extra dependency for behavior Bun already provides.

## Checkpoint 2

**GitHub repo made public**: GitHub Free doesn't allow required status checks (branch protection or rulesets) on private repos. Confirmed with the user; also reasonable for a work sample meant to be shared with an interviewer.

**CI runs `bun run check` only, no DB service**: `tsc`/`eslint`/`knip` never touch Postgres. DB-backed tests (Checkpoint 4) will use Testcontainers, which spin up their own ephemeral Postgres rather than a shared CI service container.

**Prisma client generation moved to a `postinstall` hook** (`packages/db/package.json`): CI does a from-scratch `bun install` with no prior `migrate dev` step, so `@prisma/client`'s types silently fell back to `any` until generated — caught by CI failing on `packages/db/src/client.ts`, not locally, since local `node_modules` already had a generated client from Checkpoint 1.

**Auth transport: Bearer token, not cookies**: the web and server will live on different Railway domains, so a header-based token (via Clerk's `getToken()`, bridged into the oRPC client's `headers` callback) avoids cross-origin cookie/`credentials` complexity entirely.

**Reusable `protectedProcedure` middleware** (`packages/contracts/src/procedures.ts`) over ad hoc per-procedure auth checks: uses oRPC's `os.$context<AppContext>()` + `.use()` to throw `UNAUTHORIZED` once, centrally, so Checkpoint 3's real procedures (`createPlan`, `logSession`) just extend `protectedProcedure` instead of re-checking `context.auth` everywhere.

**`me` procedure returns only the Clerk `userId`, no DB lookup**: syncing Clerk users into the `User` table (get-or-create by `clerkId`) happens naturally in Checkpoint 3 when `createPlan` first needs a real `userId` FK — doing it now would be speculative.

**`react-router-dom` for client-side routing**: introduced now because a public `/` vs. protected `/dashboard` split needs distinct URLs, and Checkpoint 3 adds more pages regardless.

**`db:deploy` script instead of `bunx prisma migrate deploy`**: `bunx prisma` from the repo root fetched a fresh latest Prisma (v7, breaking schema-format change) rather than the workspace's pinned v6 devDependency. `bun run --filter '@firstloop/db' db:deploy` resolves the local pinned binary correctly.

**Prisma migration runs at server *start*, not Railway's build step**: Railway's build containers can't reach `*.railway.internal` hostnames — that private network is only available to running/deployed services. `prisma migrate deploy` is chained into the server's start command instead; it's idempotent (no-ops with no pending migrations), so this is safe on every deploy.

**Server listens on `PORT` (Railway's convention) falling back to `SERVER_PORT`**: added `PORT` as an optional env var that wins when present; `SERVER_PORT` stays the local-only default so it doesn't collide with tooling that injects its own `PORT` for local preview.

## Checkpoint 3

**Persistent nav shell (`AppShell`) added ahead of schedule**: discussed with the user before building — a public `/` vs. protected pages already needed structure, and future sections (nutrition, strength tracking) the user wants to add later get a slot to land in without a nav restructure. Cheap now, disruptive later.

**`packages/plan-engine` has zero dependencies, not even on `@firstloop/db`**: originally imported Prisma's `DayOfWeek`/`WorkoutType` enums for convenience. Adding the seed script (which needs both Prisma writes and `generatePlan()`) would have made that a circular workspace dependency (`db → plan-engine → db`). Fixed by having plan-engine define its own string-literal-union types that mirror the Prisma enums by value — genuinely standalone now, matching what "swappable module" was supposed to mean in the first place.

**One active plan per user, most-recent-wins**: `getDashboard`/`createPlan` don't do multi-plan selection UI. `TrainingPlan` is still one-to-many from `User` in the schema, so old plans aren't lost — just not surfaced yet. Simplest thing that works for a single-plan-at-a-time product today.

**Plan `startDate` truncated to midnight UTC at creation**: found via manual testing — `new Date()` (with time-of-day) as `startDate` combined with midnight-aligned session-log dates (from a plain `<input type="date">`) meant a session logged on a plan's creation day could fall *before* that plan's own week boundary and silently vanish from "this week." Both now align to day boundaries.

**Seed script hardcodes the demo user's email instead of calling Clerk's API**: avoids `packages/db` needing `@clerk/express`/`CLERK_SECRET_KEY` just to run a one-off script — the real `getOrCreateUser` path (used by every actual request) still fetches the authoritative email from Clerk on first real login.

## Checkpoint 4

**`packages/contracts` gets an explicit `exports` map with a `./schemas/*` subpath**: the intake form needed a real (value, not type) import of `createPlanInputSchema` for client-side validation. Importing anything through the main barrel (`@firstloop/contracts`) risks pulling in `router.ts` — which imports `@firstloop/db`, and `db`'s `client.ts` instantiates a real `PrismaClient()` at module scope. Whether a bundler tree-shakes that away is not something to gamble on for "does server code end up in the browser." The `./schemas/*` subpath resolves straight to a schema file, never touching `router.ts`. Verified by grepping the built client bundle for Prisma/Clerk-backend strings (zero matches).

**`packages/contracts`'s schemas no longer import Prisma enum values from `@firstloop/db`**: `session.ts`/`dashboard.ts` used `z.nativeEnum(WorkoutType)` from `@firstloop/db` — same PrismaClient-instantiation risk as above, since importing the enum meant loading `db`'s `client.ts` module. Replaced with local `z.enum([...])` schemas (`schemas/enums.ts`) mirroring the same values, same pattern already established for `packages/plan-engine`.

## Checkpoint 6

**Light is the primary, designed identity — not dark**: the app has no dark-mode toggle today and light is what a reviewer actually sees; dark tokens still extend the existing `.dark` scaffold via `prefers-color-scheme` for completeness, but weren't the design's starting point. Register: an instrument panel / GPS watch face, not a wellness app, for a persona (lifter + cyclist + first-time marathoner) who already lives inside training data.

**Signature element is a phase-arc, computed client-side, Dashboard-only**: `apps/web` now depends on `@firstloop/plan-engine` directly (previously server-only) to call `computePhaseBoundaries` — a pure, zero-dependency function — so the arc reflects each plan's actual base/build/peak/taper proportions instead of a hardcoded shape. No new data fetching; it lives only on Dashboard, not the nav shell, so `AppShell` stays presentational and doesn't need its own query.

**Three type roles (Big Shoulders Display / IBM Plex Sans / IBM Plex Mono), loaded via Google Fonts `<link>`**: deliberately not Inter or Space Grotesk. Numbers — mileage, pace, duration, RPE, dates — get their own monospaced, tabular register everywhere, distinct from prose; this was judged the single highest-leverage typographic decision for an app whose users already read a watch face all day.

**Log-a-run form got the token/type/layout system but not a full ShadCN rebuild**: full validation + component rebuild for that form is explicit Checkpoint 7 scope (closes issue #12). Checkpoint 6 restyled it in place (colors, type, focus states, Card wrapper) so it's visually consistent with everything else in the meantime, without front-running the next checkpoint's work.

## Checkpoint 7

**Session history is a real `/history` page, not just a polished weekly list**: the brief's "session history view" was ambiguous between styling the existing "Logged this week" list and building actual multi-week browsing. Asked the user directly — they chose the full page. New `getSessionHistory` procedure returns all of a user's session logs, most recent first, no pagination (data volume for one runner's training cycle doesn't need it). This meaningfully overlaps with issue #11 (dashboard only shows the current week) but doesn't close it — #11 is also about browsing *planned* past/future weeks, which this doesn't touch, so it's left open with a progress comment instead of closed.

**Dashboard's weekly mileage chart extends `getDashboard` instead of adding a new procedure**: planned-vs-actual turned out to be a clean lift — `prescription.distanceMiles` already exists per planned workout, and the data's one query away from what Dashboard already fetches on mount. Returns the full `1..totalWeeks` array (not just weeks-so-far) so the chart shows the whole planned arc with actuals filling in as weeks pass, consistent with how PhaseArc already shows the whole plan's shape rather than just "so far."

**New test types (Testcontainers, Playwright) stay out of `bun run check`, but are still CI-required**: both add real external dependencies — Testcontainers needs Docker, Playwright needs the whole app running — that would slow down every local check. Added as separate `test:integration`/`test:e2e` scripts and separate `integration`/`e2e` CI jobs alongside `check`, all three required in branch protection. `bun run check` keeps doing exactly what it did before.

**Testcontainers integration test uses dynamic imports, not static ones**: `@firstloop/db`'s Prisma client is a module-scope singleton built from `DATABASE_URL` at import time. Since Testcontainers only knows the container's port after it starts, the test starts the container first, sets `DATABASE_URL`, *then* dynamically `import()`s `@firstloop/db`/`@firstloop/contracts` so the singleton picks up the right connection string. Also mocks `@clerk/express`'s `clerkClient.users.getUser` via `bun:test`'s `mock.module`, so `getOrCreateUser`'s create path is exercised for real without the test suite depending on live Clerk network calls — same principle already planned for Checkpoint 9's AI Coach mock.

**Playwright's e2e test lives in a root-level `e2e/` directory with its own `tsconfig.json`, not nested under `apps/web`**: it exercises the whole integrated system (server + web + Postgres + Clerk), not one package, so it doesn't belong to any single workspace. The dedicated `e2e/tsconfig.json` (and moving `playwright.config.ts` into `e2e/`) was also a practical fix — Playwright's own TS loader couldn't resolve the root `tsconfig.json`'s solution-style project references the way `tsc -b` does, and choked trying. Auth uses `@clerk/testing`'s official Playwright helpers (ticket-based sign-in by email against the existing dev Clerk instance) rather than driving the interactive sign-in modal.

**Playwright test asserts on a per-run-unique duration value, not notes text**: first attempt used a unique marker string in the session's `notes` field, but neither Dashboard nor History render `notes` at all, so the assertion could never find it. Switched to a duration value derived from `Date.now()`, which *is* rendered — and made it unique per run (not a fixed constant) after discovering a fixed value collided with itself on a second local run against the same persistent dev database, since Playwright's `webServer` reuses whatever's already running locally rather than a fresh DB per run like CI gets.

## Checkpoint 8

**Attempted a pre-prod/prod split, reverted it — staying on a single Railway environment**: built out the repo/CI side (a `prod` branch, matching branch protection, a CI push trigger, Railway-Environments-vs-Clerk-instance-split decisions logged and later refined) before discovering Railway requires a paid plan to run a second environment. Decided against paying for a second environment purely to demonstrate the pattern on a take-home project — the cost isn't justified by what it'd prove here. Reverted the `prod` branch, its branch protection, and the CI trigger change; this project deploys to the one Railway environment that's been live since Checkpoint 3, tracking `main`, and stays that way going forward. A real, plain engineering tradeoff: infrastructure cost vs. a nice-to-have pattern that doesn't change what the project demonstrates.

## Checkpoint 9

**Strength scheduling lives in its own zero-dependency package, `packages/strength-engine`, not folded into `plan-engine`**: mirrors `plan-engine`'s exact design (pure functions, no DB, program defined as swappable data) for the same reason — a future different program is a data change, not a rewrite. It doesn't import `plan-engine`'s types either; the scheduler takes a plain `WeekContext` shape, so it stays a genuinely generic "place a program's sessions around fixed days" capability, not code specific to Glute Gladiator.

**"Down/deload week" maps to the running plan's existing `taper` phase**: discussed with the user before building. Plan-engine has no periodic within-block cutback-week concept (mileage grows monotonically through base/build, holds at peak, then tapers) — adding one would touch working, already-tested running-plan behavior just to serve strength scheduling. Taper is a real, already-computed reduced-load signal, so the scheduler keys off that instead.

**Strength sessions claim REST-day placeholders, never RUN or BIKE days**: `generatePlan` already fills every day of the week (RUN, then BIKE, then REST catches whatever's left) — removing the old naive `liftDaysPerWeek`-driven LIFT block just means REST fills those days instead. The orchestration in `createPlan` treats each week's REST rows as the scheduler's available slots and drops any REST row the scheduler actually claims before persisting, so RUN/BIKE days are never touched.

**`liftDaysPerWeek` removed from `PlanIntake`/the intake form entirely**: once a real program dictates its own weekly session count and structure, a user-chosen lift-day count doesn't mean anything anymore — same reasoning the app already applies to running (nobody picks "how many quality-run days," that's derived from phase). Every generated plan now includes the strength program automatically.

**Found via verification, not by inspection: the interference rule was silently dropping Lower A for the entire peak phase.** Spot-checking seeded data (not just unit tests) showed weeks 31–36 scheduling only 2 of the reduced 3 sessions, every single week — Upper A and Upper B, never Lower A. Root cause: this plan's specific day layout (quality runs Wed/Fri, long run Sun) leaves only Tue/Thu/Sat open during peak weeks, and all three sit immediately before a run day, so the interference rule (never place a lower session the day before a quality/long run) had no legal day to place Lower A on at all — and the scheduler's original fallback logic just dropped the session rather than degrade the rule. Fixed by adding a third fallback tier: prefer full spacing, then prefer any interference-respecting day, and only as a last resort place on the least-bad day regardless of interference — because the source program's own peak-week rule explicitly keeps Lower A in the reduced set, so silently disappearing it for six straight weeks was further from the doc's intent than the fallback is. Added a regression test for this exact scenario. This is the kind of gap that unit tests alone (synthetic, individually-reasonable week contexts) don't catch — it only showed up once real data was eyeballed across a full plan.

**Two exercises share "(block reps)" in the source doc, only one is the session's "primary" lift**: Lower A lists both Barbell Hip Thrust (explicitly "the primary progression lift") and Barbell RDL as `(block reps)`, but only Hip Thrust's notes call it primary. Modeled both as `usesBlockReps: true` (their reps resolve from the block's `mainLiftReps` at generation time) while reserving `isMainLift` for the one exercise per session the doc explicitly names — a literal reading of what's actually in the table rather than assuming "block reps" and "primary lift" are the same thing.

**Session drop order beyond the documented 4→3 cut is inferred, not specified**: the doc explicitly says Lower B drops first when cutting to 3 sessions during peak weeks, but says nothing about a deeper cut. `sessionDropOrder` in the program data continues past that point as a reasonable fallback (kept in code as an explicit "not a documented rule" comment) rather than leaving the scheduler with no defined behavior if it's ever needed.

## Post-Checkpoint-9 fix: three strength modes on intake

**`SessionTemplate.isLowerBody` split into `respectsInterference` + `spacingGroup`**: product review asked for a "custom" strength mode that respects the interference rule (no session the day before a quality/long run) but explicitly skips the 48-hour lower-body spacing rule, since we don't know which of a user's own sessions are lower-body-heavy. One boolean couldn't express "needs interference avoidance but not spacing" — Glute Gladiator happened to need both together for the same sessions, which is what let `isLowerBody` get away with doing two jobs at once. Decoupled them properly (spacing keyed by an optional group name, multiple sessions can share a group via `Map<string, number[]>`) rather than a same-boolean workaround, so "feed the same generic scheduler a minimal synthetic program" is actually true, not just true for Glute Gladiator. `SessionName` also widened from a literal union naming Glute Gladiator's four sessions to a plain `string` — a "generic" scheduler hardcoding one program's session names in its type was already a smell, custom mode just made it obviously wrong.

**Custom mode is a data trick, not a second scheduler**: `buildCustomProgram(n)` produces N sessions with `respectsInterference: true`, no `spacingGroup` (so the existing spacing check is trivially always-satisfied — nothing to compare against), `exercises: []`, and a 1-week cycle with no real block progression. Fed through the exact same `scheduleStrengthSessions` Checkpoint 9 built. No peak-mileage cutback (`fullSessionCount === reducedSessionCount`), matching how `bikeDaysPerWeek` already behaves — bike doesn't reduce during peak weeks either, so custom lifting shouldn't either.

**Verified, not assumed: Dashboard already showed real session names/exercise counts, not generic "Lift" placeholders** — this was built correctly in Checkpoint 9 and confirmed again here by reading the code before claiming it either way. What building custom mode *did* surface: `describePrescription`'s exercises check was truthy for an empty array (would've shown "Lift session · 0 exercises," reading like a bug even though it's real data), and `LogSession`'s initial `type` field was derived from whether the linked prescription had exercises rather than the actual workout type — a custom lift session (no exercises) would have silently defaulted the form to "Run." Both fixed; both would only have shown up once custom mode existed to exercise them.

## Checkpoint 11

**Generic day-placement logic extracted into a new zero-dependency package, `packages/scheduling`**: the running-frequency fix needed the exact same "place a set of named slots into a week, respecting interference and same-group spacing" capability the strength scheduler already had (`placeSessions`/`isBlockedByInterference` in `strength-engine/src/schedule.ts`) — those functions never touched anything strength-specific, only `.name`/`.respectsInterference`/`.spacingGroup`. Rather than duplicate that logic a third time or have one domain package depend on the other, pulled `Slot`/`placeSlots`/`DayOfWeek`/`WEEK_DAY_ORDER` into `packages/scheduling`, which `plan-engine` and `strength-engine` both now depend on. `plan-engine` goes from zero dependencies to one, but `scheduling` is itself pure day-of-week math — nothing DB/Clerk-shaped — so it doesn't undermine the reason `plan-engine` stayed standalone in the first place (Checkpoint 3).

**Feasibility minimums (20 weeks for a first marathon, 12 for a repeat one) are a stated coaching judgment call, not a formula**: there's no principled way to compute "enough runway" from first principles here — these are commonly-cited, conservative minimums for an injury-conscious build from a modest base. Logged plainly as judgment calls (in code comments and here) rather than presented as more rigorous than they are, per explicit instruction. The check never hard-blocks plan creation; it only names the gap.

**Feasibility warning is computed once at plan creation and persisted in `TrainingPlan.config`, not recomputed live on every dashboard load**: mirrors the existing `strengthMode`/`customLiftDaysPerWeek` pattern already stored there. `getDashboard` reads it back out and returns it so it stays visible as a persistent banner rather than a one-time toast. Noticed in passing that the *existing* injury-flag warnings from `generatePlan` have the same one-time-only gap (returned in `createPlan`'s response, never persisted or re-shown) — left that alone since fixing it wasn't in scope for this checkpoint, but it's now a known, named gap rather than an unnoticed one.

**Quality-run slots get a `spacingGroup` even though only interference-avoidance was explicitly requested**: without some minimum spacing between two quality-run days, a peak-phase week could land tempo and intervals on adjacent days — worse than the old hardcoded Wednesday/Friday, which were always exactly 2 days apart. Reused the same `spacingGroup` mechanism `placeSlots` already provides (the exact thing built for lower-body spacing in Checkpoint 9) rather than inventing a second constraint system, and kept the 2-day minimum as a floor that preserves the old fixed placement's spacing without hardcoding the days themselves.

**Easy-run duration is a flat 30 minutes, not derived from mileage**: matches how quality runs already work in this codebase (flat 40/45 minutes, not mileage-derived either) — consistent with the existing model's level of precision rather than introducing a new one just for easy runs.

**Running progress buckets by calendar week (Monday-start, UTC) across a user's entire logged history, not by plan week**: "over the logged history" reads as everything a runner has logged, and a runner's history can span more than one plan (the app already allows creating a new plan; old `PlannedWorkout`/`SessionLog` rows aren't deleted). Average pace per week is mileage-weighted (total logged minutes ÷ total logged miles for runs with both fields) rather than an average of each run's individual pace, so one very short run doesn't skew a week's number as much as it would in a naive average.

## Checkpoint 12 — Full plan view

**Renamed the existing `/intake` nav link from "Plan" to "Goal"**: this checkpoint's whole point is a page that actually shows the plan, so giving that the "Plan" label meant the existing intake link (which sets your race goal, not a plan view) needed to stop using it — two nav items both called "Plan" pointing at different pages would have been confusing on day one, not a hypothetical problem to fix later.

**Layout is tabs by phase, accordion by week within a tab — decided against the real 16/14/6/3 week split, not chosen up front**: a flat 39-week accordion is too long to scan, and week-level tabs (no phase grouping) don't read as an arc. Tabs give a 4-click-max "jump to any point in the plan" structure that mirrors how `PhaseArc` already segments the plan visually; each phase's own week count, even base's 16, is fine as one-line collapsed accordion rows. The current week's accordion item opens by default and gets the same `--flare` treatment `PhaseArc`'s current-week marker already uses, so both "you are here" signals share one color language instead of inventing a second one.

**New `getPlanOverview` procedure shares a `computePlanMeta` helper with `getDashboard`, rather than duplicating the totalWeeks/currentWeek/phase/feasibilityWarning computation a second time**: both procedures need the exact same derived fields from a plan row; pulled that into one function in `router.ts`. Behavior-preserving for `getDashboard` — confirmed by its existing integration-test assertions still passing unchanged. Also extracted `describePrescription` (previously local to `Dashboard.tsx`) into `apps/web/src/lib/prescription.ts` so the new Plan page renders workout rows with identical formatting instead of a second copy that could drift from the original.

**Full plan view stays fully read-only — no "Log this" action on any row, even though Dashboard's current-week list has one**: the checkpoint explicitly scoped this to read-only, same standing as the already-tracked editing/rescheduling issue (#10). Omitting the action entirely (rather than, say, disabling it) keeps the page's purpose unambiguous — it's for seeing the arc, not for logging against it.

**Correction (established in Checkpoint 17): the premise in the entry above was false when it was written.** Dashboard's current-week list carried the "Log this" action for `LIFT` sessions only — every run, including the long run, had none — so the matched pair the entry leans on never existed. The decision itself still stands on its own merits and is unchanged; only the justification cited for it was wrong. Left in place rather than rewritten, because the entry is the record of what was believed at the time. Full detail in Checkpoint 17.

**Observed via this view, not fixed by it: peak-phase weeks sometimes place fewer strength sessions than the program's own "reduced count" of 3.** Browsing every week surfaced that once running (Checkpoint 11) claims 4 days in a peak week (long run + 2 quality + 1 easy) and bike claims 1 more, only 2 days are left for strength — one short of the 3 the program's peak-cutback rule targets. This is the same honest, pre-existing truncation behavior already documented for `bikeDaysPerWeek` and custom strength mode ("if a week's available days can't fit every session, fewer get placed") — not a new bug, and not something a read-only view is the right place to fix. Worth naming here as a real interaction between two separate checkpoints (11's higher running frequency, 9's strength scheduler) that only became visible once the whole plan could be browsed at once, exactly the kind of gap this checkpoint's own verification loop is supposed to surface.

## Checkpoint 13 — Safety and correctness remediation

Prompted by a coach-persona and designer-persona review of the actual generated output (not the code in the abstract) — both reviews are recorded in full in this session's transcript. Four real problems came out of it; this checkpoint fixes them and only them. The review's other findings (no cutback weeks in base/build, the fixed 19mi peak long-run distance regardless of experience or current mileage, static block-level strength progression with no double progression, Custom mode's missing explanatory caption, interference rules being invisible in the UI) are explicitly *not* addressed here — they're documented as known limitations in the Checkpoint 14 write-up instead of silently dropped.

**`intensityRank` is a new axis on `BlockDefinition`, deliberately decoupled from the existing `isDeload` boolean.** Glute Gladiator's "Test/Deload" week is `isDeload: true` (a volume-reduction week by the program's own accounting) but its actual prescription is "work to a heavy top set on hip thrust, squat, RDL, bench, row" — near-maximal effort, not a light week. Capping peak-mileage running weeks by `isDeload` alone would have let that week through unmodified, since it already reads as "deload." `intensityRank` (Deload=0, Build=1, Strengthen=2, Peak=3, Test/Deload=4) measures actual load instead, and a new `isPeakMileageCap` flag designates Strengthen as the block a peak-mileage week steps down to when the raw cycle position would land on something above it. Verified against the app's own seeded 39-week demo plan: weeks 33-35 (raw "Peak") and week 36 (raw "Test/Deload", the near-1RM testing week that was landing on the single highest-fatigue week of the whole plan) now both resolve to "Strengthen."

**`checkDayEconomy` warns about two distinct failure modes — understaffed weeks and unsafe spacing — with no minimum-affected-week threshold before it speaks up.** A week counts as understaffed if fewer sessions got placed than the program intended (session count, not exercise-level), and as unsafe-spacing if two sessions sharing a `spacingGroup` land closer together than `minDaysBetweenGroupedSessions`. Even a single affected week triggers a warning, worded proportionally ("2 of 39 weeks" vs. "39 of 39 weeks") rather than suppressed as noise — the whole point was "don't silently degrade," so a real gap that only shows up once still gets named. Computed once at `createPlan` (and in the seed script, mirroring `feasibilityWarning`'s existing pattern) and persisted in `TrainingPlan.config`, not recomputed live. Reseeding the demo persona (4 running days + 1 bike day + program mode — the app's own defaults) now produces a real, honest warning: 39 of 39 weeks understaffed, 33 of 39 with Lower A/Lower B back-to-back. Deliberately did not change the demo's chosen inputs to dodge this — a visible warning on the seeded plan is the proof the fix works, not a bug to hide before submission.

**Checked `docs/strength-program.md`'s actual "Swap Table" before building anything against it — it's titled "(Equipment or Preference)" and is genuinely not injury-oriented.** Of its five entries, exactly one is defensible for a knee flag: "Back Squat → Front Squat, Hack Squat, or Leg Press," and of those three, only Leg Press (fixed path, less stabilization demand) is actually easier on the knee — Front/Hack Squat aren't, and I'm not claiming otherwise just because they're in the same table row. The other two knee-loading movements in the program, Bulgarian Split Squat and Walking Lunge, have no documented alternative anywhere in the source doc, and Glute Gladiator is explicitly the literal, verbatim source of truth per Checkpoint 9's own constraint — inventing replacement exercises that aren't in it isn't on the table. Those two get dropped from the session for a flagged knee instead, which is the honest version of "no defensible substitute exists" rather than a fabricated one. For "IT band," "Shin splints," and free-text "Other" flags: no exercise-level change at all — nothing in the doc or in defensible general judgment gives a groundable substitution the way the swap table does for knee, and saying plainly that nothing was adapted beats pretending a change happened. This is stated outright in the persisted injury note rather than left to silence.

**The persisted injury note is new, separate router-level logic, not an extension of `plan-engine`'s existing transient `warnings` array.** `generatePlan`'s per-flag messages (unchanged, still returned once from `createPlan`) don't know anything about strength — they're running-only. The new `injuryWarning`, stored in `TrainingPlan.config` and read back by the same `computePlanMeta` helper `feasibilityWarning`/`strengthWarning` already use, is a router-level concern precisely because it's cross-cutting (it names both the running mileage reduction and the strength-side adaptation status in one sentence). It reuses plan-engine's own already-worded running sentences rather than reconstructing the reduction percentage independently, so the two can't drift out of sync.

**Home page's ping health-check box removed outright, nothing put in its place.** It was Checkpoint 1 scaffolding that got restyled into the Checkpoint 6 card treatment in passing but never actually removed — the backend `ping` procedure itself stays (a legitimate lightweight health check), just no longer surfaced as UI.

**Noted in passing, not fixed here: `SessionLog` rows survive a plan's deletion during reseeding (`onDelete: SetNull`, not `Cascade`), so every past `bun run --filter '@firstloop/db' db:seed` has left behind orphaned freeform logs from whichever plan existed at the time.** Spotted several with implausible multi-thousand-minute durations while spot-checking the reseeded demo plan for this checkpoint — pre-existing seed-script data hygiene, unrelated to any of today's changes, and out of this checkpoint's explicit scope. Worth a cleanup pass before the final submission push in Checkpoint 15.

## Checkpoint 14 — Stretch features (nutrition, AI coach)

**Both stretch features got built, nutrition first, on the checkpoint's own priority ordering.** Nutrition was the cheaper of the two and was finished before the coach was started, so a blowup on the API integration couldn't have cost both features. Explicitly out of scope and still future work, unchanged from the checkpoint brief: a tool-using coach, an agentic CI review step, Terraform.

**The nutrition page is static content with no state, no input, and no calculator — the ranges are hardcoded and deliberately coarse.** A per-runner carb target would need bodyweight, sweat rate, GI tolerance, and race pace, none of which the app collects and none of which it should start collecting for a reference page. Coarse published ranges by long-run duration are the honest version of what this page can actually know; a narrower number would imply a precision the feature isn't doing the work to earn. The page says outright that nothing on it is personalized and points at a professional for individual advice, rather than letting placement inside a training app imply otherwise.

**The AI coach comments on training; it is not what makes training safe.** `plan-engine` and `strength-engine` already enforce progression, taper, peak-week intensity caps, spacing, and injury adaptation at generation time — all of it deterministic and unit-tested. The coach reads the last two weeks and says how it went. This split is why the endpoint is allowed to fail: an outage costs a paragraph of commentary, not the correctness of anyone's schedule. Nothing in the system prompt asks the model to modify the plan, and it's told not to.

**No API key is a first-class supported state, not an error.** `createAnthropicCompletion` returns `null` when `ANTHROPIC_API_KEY` is unset and the endpoint returns `status: "unavailable"` before doing any database work — so a fresh clone, CI, and a local dev environment all run the whole app with no key and no failures. Any exception from the live call is caught and collapses to the same state, because a coach outage must not take the dashboard down with it. `ANTHROPIC_API_KEY` is documented as optional in `.env.example` for exactly this reason.

**The Anthropic call sits behind a `CoachCompletion` function type, which is the seam the tests substitute.** Everything above it — the 14-day window math, adherence counting, missed-session detection, mileage bucketing, prompt assembly, response parsing — is pure and unit-tested with no network and no key (`packages/contracts/src/lib/coach.test.ts`, in the fast `bun test` path). The SDK is `import()`ed lazily inside the completion, only once a key exists. CI therefore needs no key and incurs no cost, per the checkpoint's explicit constraint.

**Structured output via `output_config.format` with a raw JSON Schema, parsed and then re-validated with Zod.** The schema constrains the model to `{guidance, concern}` with `concern` nullable, so "nothing to flag" is a representable answer rather than something the model has to phrase its way out of — the prompt tells it not to manufacture a concern, and the schema gives it somewhere to put that. The response is still `JSON.parse`d and run through a Zod schema on the way out; a malformed response fails loudly into the caught path instead of rendering as guidance. Used the raw JSON Schema rather than the SDK's Zod helper to avoid coupling the contract to a specific Zod major version.

**The coach is button-triggered on the dashboard, not fetched with the rest of the page.** It is the only call in the app that costs money per invocation, so asking for it is an explicit act. Fetching it automatically on every dashboard load would bill a request for every navigation, including ones where nobody reads it.

**The seed script now deliberately leaves the most recent past long run unlogged.** It previously relied on a 12% random skip, which meant a demo could easily land on a persona with perfect adherence and a coach with nothing to say. Choosing the miss explicitly — and logging which date it chose — makes the demo reproducible and gives the concern path something real to find, which is what the checkpoint asked for.

**Re-ran the Checkpoint 4 bundle-leak check after adding `@anthropic-ai/sdk` to `packages/contracts`, because that's exactly the shape of dependency that caused the original problem.** Built the web client and grepped the output: zero occurrences of `PrismaClient`, `prisma`, `@anthropic-ai`, `anthropic`, or `api.anthropic.com`. The one `ANTHROPIC_API_KEY` hit is the literal string in the coach card's "not configured" copy, not a value or an import. The `./schemas/*` subpath rule that made this safe the first time is what keeps it safe now — the web app value-imports the new coach schema through `@firstloop/contracts/schemas/coach`, never through the root barrel.

**"No key configured" and "the call failed" are separate states, split after the first one misled a real debugging session.** Both originally collapsed to `status: "unavailable"`, so a correctly-configured key whose account had no credit rendered as "The coach isn't configured in this environment. Set ANTHROPIC_API_KEY…" — the single most misleading thing to show someone who had just set `ANTHROPIC_API_KEY` correctly. They're now distinct: `unavailable` means no key (fix is a config change), `failed` means a key exists but the call didn't succeed — bad key, exhausted credit, upstream outage, malformed response — and points at the server log, which already carried the real reason. The cost of conflating them isn't theoretical; it sent the first person to configure this feature looking in exactly the wrong place.

**Added `e2e/stretch-features.spec.ts` rather than verifying by hand.** The coach assertion is written key-agnostically: it checks the request *settles* (the button returns to an enabled "Ask again" and the loading state clears) rather than asserting on guidance text, so the same test passes with or without a key configured and never depends on model output being stable. That also pins the real requirement — a coach outage must never leave the dashboard stuck loading.

## Checkpoint 15 — Systems diagram and write-up

**Not logged when it happened, recorded here: the orphaned-`SessionLog` cleanup shipped in commit `2301045` during Checkpoint 14, but no decision entry was ever written for it.** Checkpoint 13 flagged it as "worth a cleanup pass before the final submission push" (above) and then the fix landed as a bug fix without the log catching up — the exact drift the decisions log exists to prevent. The substance: `SessionLog.plannedWorkoutId` is `onDelete: SetNull`, so deleting the previous plan on reseed orphaned its logs instead of removing them, and mileage is queried by user (correctly — a runner's history legitimately spans plans), so every past reseed stacked another full history on top of the last. 450 orphans inflating weekly totals to 80+ miles on a plan whose longest run is 9.9mi, plus junk rows carrying 57,857-minute durations. Found because the AI Coach read those totals back as real training load and reported them as genuine mileage. Fixed by deleting the demo user's `SessionLog` rows in the seed script alongside the plans, rather than changing the FK to `Cascade` — cascade would be wrong for the app itself, where a logged session outliving the plan it belonged to is correct behavior.

**Both Mermaid diagrams live inline in `README.md` rather than as separate `.mmd` files or committed images.** GitHub renders Mermaid natively in Markdown, so a fenced block in the README is simultaneously "committed to the repo" and "rendered in the README" with no build step and no exported asset to drift out of sync with its source. Rendering verified on GitHub itself rather than assumed from valid-looking syntax.

**Diagram A shows the request path and the pure-package boundary in one picture, not two.** `plan-engine`/`strength-engine`/`scheduling` are drawn as a subgraph hanging off the procedure handler, so the one-way dependency graph and "these touch no DB and no network" are visible in the same frame as the Prisma/Postgres edge they deliberately avoid. The Anthropic edge is dashed and labelled allowed-to-fail for the same reason — the coach's isolation is an architectural claim, so it should be readable off the diagram rather than only in prose.

**The write-up was verified against the codebase before being committed, not trusted because it was detailed.** Three things changed as a result. The bundle-leak check was re-run (two checkpoints had touched `packages/contracts` since the last one): `PrismaClient`, `prisma`, `@anthropic-ai`, `anthropic`, `api.anthropic.com`, `@clerk/express`, `clerkMiddleware`, `CLERK_SECRET_KEY`, `getOrCreateUser` — zero occurrences, with the single `ANTHROPIC_API_KEY` hit confirmed to be the coach card's own "not configured" copy. The coach's window was described as "the last two weeks," which undersells it — the snapshot carries a 14-day detail window *plus* four weeks of mileage trend, and now says so. And CI was described as required "from the very first checkpoint" when the workflow actually landed in PR #1 during Checkpoint 2.

**One claim in the draft was false and got moved rather than softened: weekly mileage is *not* currently labeled as what it measures.** The draft listed it among the shipped honesty examples. The dashboard chart says "Weekly mileage" with planned/actual in miles and draws no distinction between "this run was prescribed by duration, so there is no distance" and a genuine zero. That is Checkpoint 16 work, so it moved out of the honesty section and into the named-gaps section as a current overstatement, stated plainly. Writing it as done would have been the precise failure the section is about.

**The day-economy paragraph and its "left the warning visible rather than changing the demo's inputs" claim were committed as-is, knowing Checkpoint 16 changes exactly that.** Accurate today; the alternative was committing a write-up describing behavior that hasn't shipped. CP16 rewrites it to say both true things — the check fires correctly and was never suppressed in code, *and* the demo's own inputs changed because a correct warning still shouldn't be a new user's first impression — rather than letting the earlier claim quietly disappear.

**README retitled to Cadenza with the name split stated outright.** The repo and packages stay `firstloop-marathon-app`/`@firstloop/*` on purpose, but a README titled "FirstLoop" for an app called Cadenza reads as an oversight to a first-time reader rather than as a deliberate separation. One sentence fixes that; renaming the packages to match would have been churn for no benefit.

**Video outline is committed to the repo (`docs/video-outline.md`) and carries a "record after Checkpoint 16" note at the top.** Two of its sections demonstrate behavior CP16 changes, so the dependency belongs in the file rather than in someone's memory of this conversation.

**README's env-var table was corrected against `apps/server/src/env.ts` rather than transcribed from `.env.example`.** The first draft listed `SERVER_PORT` and `WEB_ORIGIN` as required; the Zod env schema gives both working defaults (`3001` and `http://localhost:5173`), so only `DATABASE_URL` and the Clerk pair actually fail the boot. Telling a reviewer that four more variables are mandatory than really are is the kind of small inaccuracy that makes someone distrust the rest of the setup instructions when one of them turns out to be optional.

## Checkpoint 16 — Invites and final verification

**The day-economy defaults could not be fixed the way this checkpoint's own plan said they could, and the check is what proved it.** The plan asserted that running and bike day counts were the lever and Glute Gladiator stayed untouched. Sweeping running 3-5 x bike 0-2 x program/custom-1-3 through `generatePlan` + `checkDayEconomy` on real 39-week output showed that assumption was false: the program needs 4 free days a week, every running or bike day consumes one, so **3 running days + 0 bike days is the only clean program-mode configuration in the space** — and it leaves zero rest days. Every other clean combination requires custom mode. The old default (4 run / 1 bike / program) gave 4 sessions 2 days to land on; the warning was accurate, not noise. Recording this because the instruction was to verify against real output rather than guess at numbers that seem reasonable, and doing that is what caught it — guessing would have produced a "fix" that still warned.

**Resolved by splitting the two defaults rather than picking one.** Seed: 3 run / 0 bike / program, so the populated demo dashboard shows genuine Glute Gladiator sessions (all four placed, Wed/Thu/Fri/Sat) — the persona gives up its bike day to keep the flagship strength feature visible. Intake form: 3 run / 1 bike / custom-2, so a new user's first plan keeps all three disciplines *and* a real rest day, which is the more defensible plan qua training plan. Both verified `warning === null` from the real code path, and the intake defaults verified end-to-end through the actual form by the e2e golden path (which fills only race date and mileage, leaving every other field at its default) — the persisted plan config came back with all three warnings null. `checkDayEconomy` itself was not thresholded, softened, or touched.

**Seed derives distance for duration-only runs in the log, and deliberately not in the prescription.** Every run except the long run is prescribed by duration, so the demo's entire mileage history was previously just its long runs. `simulateActuals` now derives distance from the logged duration and a quality-appropriate pace (easy 9.3-10.6 min/mi, tempo/intervals 7.6-8.6). This is legitimate in a *log*, which simulates something that happened and which a real runner would know the distance of; writing it back into the `prescription` would be inventing plan content `plan-engine` never produced. The planned side still has no honest distance for those runs, and that limitation stays named in the write-up rather than papered over by the seed change.

**`weeklyMileageTotal` and `totalMiles` became nullable, reusing the convention `averagePaceMinPerMile` already established** rather than inventing a second one: null means "nothing to measure," `0` means "genuinely nothing." A week whose runs were all logged by duration now renders `—` plus "Not measured in miles" instead of a `0.0` that reads as a rest week. Verified by stripping distances off a week's logs in the local DB and confirming the rendered output, since seeded data (post-fix) never produces the null case on its own.

**The planned-vs-logged chart is long-run distance on both sides, and says so in its heading.** Once the seed derived distances, logged miles counted every run while planned miles still only counted long runs — a comparison of two different quantities that would have rendered as a large weekly overshoot. The actual series is now restricted to logs whose linked `PlannedWorkout` actually prescribed a distance. This is the labeling fix the checkpoint asked for, arrived at from the opposite direction: the honest label forced the honest query, not the other way round.

**Found while verifying the above: the chart drew every future week as logged `0`.** With 39 weeks of plan and 9 elapsed, weeks 10-39 rendered as a flat zero line, which reads as thirty consecutive missed weeks. Pre-existing, and only visible once the mileage numbers were being looked at honestly. `actualMiles` is now null beyond the current week — a *past* week with no logged long run is still a real `0`, because that's the adherence signal the chart exists for.

**`LogSession` has no empty state and correctly needs none** — checked rather than assumed, since it was the one protected page without one. It never fetches a plan; it's a standalone freeform form, so a brand-new user with no plan gets a fully working log page. Dashboard, Plan, Progress and History all have real empty states, verified by emptying the demo user's plans and logs and walking every protected route.

**Open issues #9, #10, #13 and #15 stay tracked, not fixed.** All four are genuine enhancements (periodization heuristics, edit/delete for logged sessions and plans, loading skeletons and form error handling, multi-plan selection), none is trivial, and all four are already named in the write-up's out-of-scope section. Triaged, not silently deferred.

## Checkpoint 17 — Navigation flow and dashboard logging fix

**Diagnosed before fixing, and the answer was none of the likely-sounding ones: the "Log this" action was present but only ever rendered for lift sessions.** `Dashboard.tsx` gated it on `w.type === "LIFT" && w.prescription.exercises`; every other type fell through to an empty `<span />`. So on a typical week the four Glute Gladiator sessions had the action and all three runs — including the long run, the most important session in the week — had no affordance at all. Not a Checkpoint 13 regression, which was the first suspicion: `git log -L131,132 -- apps/web/src/pages/Dashboard.tsx` returns exactly one commit, `3dc19cf` (Checkpoint 9, PR #25). It was born lift-only, because its purpose then was to carry a *structured lift prescription* into the log form, not to be a general "log this planned workout" action. Fixed by gating on `w.type !== "REST"` instead. No server-side change was needed at all — `logSessionInputSchema` already accepted `plannedWorkoutId` and `LogSession` already read and submitted it; only the button was missing.

**Checkpoint 12's stated justification for the read-only plan view was wrong when it was written, and is corrected here rather than quietly left standing.** That entry reads "no 'Log this' action on any row, even though Dashboard's current-week list has one," and leaned on the two being a matched pair. The pair didn't exist: Dashboard had it for lifts only. The *decision* — keep the full plan view read-only so its purpose stays unambiguous — is still right and stands. The premise cited in support of it was not true.

**The real cost of this bug was downstream, in the coach, and it was invisible in the demo.** `buildTrainingSnapshot` builds `loggedPlanIds` from each log's `plannedWorkoutId` and reports any past planned session not in that set as *missed*. A runner logging every run faithfully through the generic "Log a session" button produced logs with `plannedWorkoutId: null`, so the coach would have told them they missed every run they actually ran. The seed script sets the link directly when it creates its history, which is exactly why eight weeks of demo data never surfaced it — the only path that reproduced it was a human logging a run through the UI. Verified fixed by logging Monday's easy run from its dashboard row and confirming the persisted `SessionLog` came back linked to week 9's Monday `PlannedWorkout`, not as a freeform row.

**Planned duration is prefilled into the log form when arriving from a dashboard row.** The lift path already arrived carrying its whole prescription; a run arrived with an empty duration field, asking the runner to retype the 30 minutes the row had just displayed. Prefilled from `navState.prescription.durationMin` and still fully editable — it's what was planned, not a claim about what happened. Distance is deliberately *not* prefilled: the plan has no distance for duration-prescribed runs, which is the same limitation Checkpoint 16 labeled honestly rather than papered over.

**Nav item renamed "Plan" → "Schedule".** Every other item is a single noun (Dashboard, Log, History, Progress, Fueling, Goal); "Plan" sitting two items from "Goal" reads as a verb — *plan your training* — when the page is strictly a read-only view of the plan you already have. One-line change, no test or copy depended on the old label.

**Nav order left alone; the first-run problem is fixed by routing instead.** For anyone who has a plan — the state a user is in for 30-plus of the 39 weeks — the existing order already is the real sequence, and Dashboard belongs first. The incoherence was specific to having no plan yet: "Goal" is last, so nothing draws a new user to the only page that helps them, and generating a plan then redirects to Dashboard, running the interaction backwards against the nav. `Dashboard` and `Plan` now `<Navigate to="/intake" replace />` when `plan` is null instead of rendering a "no plan yet" dead end. Deliberately not implemented as a check in `AppShell`, which Checkpoint 6 kept presentational and query-free on purpose; both pages already fetch and already branched on this exact condition, so routing from them adds no query.

**Video outline restructured around a stated thesis, and a missing section added.** The brief's original success criteria list "verification tooling running locally and in CI/CD" alongside the monorepo and the deployment, and the outline described CI in one passing clause and never showed it — the single largest gap against the actual ask. It now has its own section, placed deliberately *before* the bug stories rather than next to the architecture: an audience that has just watched `bun run check` and three required CI jobs go green understands what the logging bug got past, so the failure reads as informative rather than embarrassing. Paid for by cutting the golden path from 2 minutes to 1:15 — it's the least distinctive content in the video and now carries setup for later sections rather than making its own argument. Total drops from ~8.5 to ~7:20, inside the 5-8 minute target. Also added an explicit through-line stated in the open and closed in the last 30 seconds ("everything here is checkable, and the places where the system proved me wrong are in the record"), since three separate sections are evidence for that one claim and the outline previously left it implicit.

**Within that section, the logging bug leads rather than being added as a third item.** The outline already ran to roughly 8.5 minutes against a 5-8 minute target, so a third story of equal weight would have pushed it over. Led with this one because it's the strongest of the three: nothing automated could have caught it, the decision log had confidently asserted the opposite, and the consequence (the coach reporting faithfully-logged sessions as missed) was invisible precisely because the seed fixture that makes the demo realistic also sets the link that hid the bug. Section 3's golden path was tightened to log a run *from its dashboard row* specifically, so section 6 has a concrete click to call back to and can carry part of its own setup.

**Checkpoint 17's fix shipped without a regression test; added here after verifying the two factual claims in the walkthrough script.** Checking the script's "ninety tests" figure turned up something more interesting than a stale number: `git diff --stat 9e322e7..HEAD -- '*.test.ts' 'e2e/*.spec.ts'` is empty, so no test file had been touched since Checkpoint 14 — Checkpoint 15 was documentation, and 16 and 17 both changed real behaviour and added nothing. The count was still accurate at 90, but a video whose strongest section is "here's a bug every test missed" invites the obvious follow-up question, and the honest answer was that there was still no test for it. `e2e/log-from-plan-row.spec.ts` now asserts a run row offers the action, that a REST row still doesn't, and that the prescription travels with the click (preselected type, prefilled duration, distance deliberately empty). **Verified by reintroducing the bug rather than by watching it pass:** restoring the `w.type === "LIFT"` gate fails it on the `Log this` visibility assertion — and the pre-existing golden-path test still passed while it failed, which is the concrete demonstration of why nothing caught this originally. The persisted foreign key is not asserted, since Playwright has no database access; the prefilled duration is the observable proxy, because it can only arrive via the same router state that carries `plannedWorkoutId`.

**The script's client-side feasibility claim was checked and left alone.** `Intake.tsx` imports `checkFeasibility`/`estimateAvailableWeeks` from `@firstloop/plan-engine` and computes them in a `useMemo` keyed on race date and experience, rendered directly — genuinely client-side, and not a confusion with Checkpoint 6's phase-arc import, which is a separate call site. Checkpoint 11's server-side persisted warning is also real; both exist deliberately, the same pure function called from both sides. The line was sharpened to name both rather than let "runs in the browser" imply it's the only one.

**Walking the app to write the script surfaced that the demo account had accumulated 10 plans, and the dashboard was showing none of the seeded history.** Every e2e run calls `createPlan`, and the app resolves "most recent plan wins," so each run buried the seeded nine-weeks-in persona under a plan generated that day with no history behind it. Reseeding became step 1 of the recording notes with the reason stated, and the two sections that themselves create a plan (3 and 5) are now explicitly scheduled last, with a table of which section needs which data state. Recording this because it is a property of the verification tooling, not of the video: **running e2e against any environment leaves that environment's demo account showing a fresh empty plan**, which matters for the deployed demo as much as for a local one.

**The script stopped reciting numbers the app computes from today's date.** Plan length is derived from the current date and had already drifted 39 → 38 weeks between the outline being written and being read back, so the script now says to read the figure off screen, and does the same for the day-economy warning's affected-week counts. A spoken number that silently expires is a worse failure than an absent one — it is wrong on camera with nothing flagging it.

**`>` now means spoken words and nothing else.** Two director's notes in section 4 were wrapped in the same blockquote used for narration, so they read as lines to say out loud, and one spoken block was missing its `Say:` header. Notes use a plain `Note —` prefix and a convention block at the top of the outline states the rule. Section 4 also gained the concrete artifacts it was missing — what to have open, the exact command, and the exact Actions run URL that shows `check`/`integration`/`e2e` green in one frame with nothing to expand. Runtime re-counted at ~1,270 words, 7:30 brisk to 8:30 measured; the AI Coach beat was cut from section 3, and the live feasibility check added as an explicit optional beat, since without typing a too-close race date first the "runs in the browser as I type" line has nothing behind it.

**A brief-coverage rubric for the walkthrough's opening was written, then closed unmerged (PR #37) — the video had already been recorded.** The proposal opened section 0 by naming the ask point by point: monorepo on the named stack, Railway as three separate services, verification locally and in CI, Clerk auth, a public repo alongside a live URL, and Terraform as the one bonus deliberately skipped. It is the most direct statement of alignment-to-brief anywhere in the repo, and it is not in the script. Closed anyway, because once the recording exists the outline stops being a plan for what to say and becomes a record of what *was* said — editing it would make it diverge from the artifact it documents, which is the opposite of why it is checked in. The commits stay reachable through `refs/pull/37/head` if the rubric is ever wanted for a written artifact, where the argument would land on its own terms rather than as narration.

**Measured before deciding, because the two branches disagreed about the runtime and only one could be right.** Counting spoken lines (`^> `) gives 1,272 words on `main` against a stated ~1,270, and 1,388 on the PR against a stated ~1,390 — both headers honest about their own branch. The 116-word gap *is* the rubric, which is what would have pushed the script from 7:30-8:30 to 8:10-9:15 and out of the 5-8 minute target, and why that PR also carried a ranked list of cuts to pay for itself. Worth recording that the apparent contradiction was arithmetic, not an error in either document.

## Post-Checkpoint-17: e2e fixture identity

**The demo account was doing two incompatible jobs, and the e2e suite kept winning.** It is both the curated showcase fixture (~8 weeks of seeded history, which is what makes the deployed app demonstrable at any moment) and the identity every spec signs in as. Each spec calls `createPlan`, the app resolves most-recent-wins, so one run buries the persona under a plan generated that day with no history behind it. This had already happened silently — the account had accumulated 10 plans by the time anyone looked — and it happened again when the suite was pointed at production to verify Checkpoint 17's fixes were live. Reseeding fixes it, which is exactly why it kept recurring: a discipline dependency that only fails when someone forgets, and the cost of forgetting lands on a recording session.

**Fixed by making the unsafe case refuse to run rather than by parameterising the email and remembering to set it.** `e2e/test-identity.ts` resolves `TEST_EMAIL` from `E2E_CLERK_EMAIL`, defaulting to the demo account, and `assertIdentityIsSafeFor` (called from `global-setup` before Clerk is touched) throws when `baseURL` is non-local and no override is set. Localhost and CI keep the demo account, which is correct: CI starts from an ephemeral database and a local run is one `db:seed` from clean. The demo account can now only be written to by a local run or by the seed script. Choosing the guard over a new default was deliberate — a dedicated Clerk user has to exist before it can be signed in as, and defaulting to one that doesn't would have broken every e2e run including CI's, so the guard lands green today and the dedicated account can be adopted whenever it is created.

**Verified the guard both ways rather than only the happy path**: `localhost`, `127.0.0.1` and an undefined `baseURL` pass; a Railway URL is refused; `https://localhost.evil.com` is refused (the pattern anchors the host rather than substring-matching "localhost"); and setting `E2E_CLERK_EMAIL` unlocks the Railway case. The specs themselves needed no other change — both plan-dependent specs already build their own plan and assert nothing about seeded history, since CI runs against a migrated but unseeded database.

## Checkpoint 18 — Step-back persona review

**The review is a file this time, not a transcript.** Checkpoint 13's persona review was, in its own words, "recorded in full in this session's transcript" — which is not in the repo and not readable by anyone starting the next phase. `docs/reviews/2026-08-persona-review.md` is the durable version, and future reviews go in the same directory. The findings themselves are cross-referenced to issues #41-#47 so triage survives independently of the prose.

**Review and triage only — nothing found was fixed here, and that was the point.** Nine findings across two personas, seven filed as issues, none implemented. Fixing them would have bundled a critical coach bug, two product decisions and three UI defects into one push, which is precisely what one-feature-per-checkpoint exists to prevent. The single exception is `.claude/launch.json`, which was fixed because it blocked the review itself (see below).

**The most important finding is a repeat of a bug class already fixed once, in a place the fix never reached.** The AI coach told the seeded persona they had run "60-70% above planned mileage" for three straight weeks. Measured against local Postgres, they ran three runs a week exactly as planned: 15.6/14.8/15.1 logged miles against 9.7/9.4/9.1 planned. The planned figure counts long runs only, because easy and quality runs are prescribed by duration and carry no distance — `coach.ts:106-109` folds them in as `p.miles ?? 0` while every logged run contributes its full distance. Checkpoint 16 fixed this exact asymmetry in the dashboard chart and wrote a caption explaining it; the caption now sits directly beneath a coach card still making the uncorrected comparison. Nothing caught it because the coach's e2e assertion is deliberately key-agnostic and only checks that the request settles. Filed as #41.

**"Custom" strength mode turned out to be three gaps compounding, not the cosmetic caption Checkpoint 13 deferred.** It renders no explanatory caption (program mode does), it produces a bare `Lift session` row with no prescription, and per-exercise logging does not exist, so the user cannot supply the content either. Deferring the caption as cosmetic was defensible when it was the only known gap; used end to end, the caption is the visible edge of a mode that does very little. Recorded because it is a case of a deferral that should be re-judged once the surrounding facts change, not because the original call was careless. Filed as #43 and #42.

**Correction (established in Checkpoint 22): the clause above claiming "per-exercise logging does not exist" is false.** It exists, and has since Checkpoint 9 — arriving at the log form from a planned LIFT row carrying prescribed exercises gives names, prescribed sets/reps, notes, per-set reps and weight inputs, and an "Add set" control, persisted through `setLog`. The review reached the opposite conclusion by testing only the freeform `/log` route and generalising. The rest of the entry stands: Custom mode does render no caption and does produce a bare `Lift session` row. The accurate version of the third gap is that **no exercise can be logged unless the plan prescribed it**, which is why Custom mode — prescribing none by design — can never reach structured logging. Full detail in Checkpoint 22 and `docs/specs/strength-logging.md`.

**Verified rather than inherited: both of Checkpoint 13's engine-level deferrals are still open, and the measurements are now on record.** Peak long-run distance is **19 mi for a novice at 8 mi/week and 19 mi for an experienced runner at 40 mi/week** — a fixed constant, confirmed by calling `generatePlan` directly rather than by re-reading the earlier claim. Zero week-over-week long-run reductions in the first 14 weeks of either plan, confirming no cutback week. For the 40 mi/week runner the entire 43-week plan moves the long run from 14 mi to 19 mi, which is the concrete consequence of the fixed peak rather than an abstract limitation.

**`.claude/launch.json` was fixed in scope, because it silently broke the app under the preview tooling.** It ran `bun run dev` directly; the preview harness injects `PORT=5173`, and the server prefers `PORT` over `SERVER_PORT`, so the API bound to 5173 alongside Vite and every authenticated page rendered "Error: Failed to fetch." This is the exact collision `CLAUDE.md` documents and the launch config did not guard against, and the first capture run of this review was wasted on it. Changed to `env -u PORT bun run dev`. Recorded because the failure mode looks like a catastrophic app bug and is entirely local tooling — a false finding that a less careful review would have filed.

**The checkpoint's own verification run found a tenth finding the personas missed, and it was diagnosed rather than retried away.** `e2e/stretch-features.spec.ts` failed twice, then passed in 11.4s once the timeout was raised — coach latency varies from ~11s to over 30s. Two defects underneath: the card has no client-side timeout, so a *slow* upstream leaves it stuck at "Thinking…" indefinitely even though a *failing* one is handled correctly; and the test's `toBeEnabled({ timeout: 60_000 })` can never wait 60s, because no test timeout is configured and Playwright's 30s default applies — the `timeout: 60_000` in `e2e/playwright.config.ts` is the `webServer` startup timeout, not a test timeout. Neither surfaces in CI, because the CI e2e job writes an `.env` with no `ANTHROPIC_API_KEY`, so the coach short-circuits instantly and its real-latency path is exercised nowhere. Filed as #48 and left unfixed, consistent with this checkpoint's scope; the flake is local-only and does not block CI.

**What held up is recorded alongside what didn't.** Checkpoint 17's plan-less routing, every empty state, `/log` standing alone without a plan, the day-economy warning ("43 of 43 weeks"), the client-side feasibility check, and the injury adaptation path all behaved exactly as documented. A review that lists only faults misrepresents the system, and the next reader needs to know which claims were checked and passed, not just which failed.

## Checkpoint 19 — The coach's mileage comparison (#41)

**Fixed by mirroring the restriction Checkpoint 16 already applied to the chart, rather than inventing a second approach.** `buildTrainingSnapshot` now builds a set of planned ids whose prescription carries a distance and counts a logged run toward the actual side only if it links to one of them — the same set-based rule as `getDashboard`'s `distancePlannedWorkoutIds`. The planned side was already long-run-only and is unchanged. No router change and no new data were needed: `PlannedItem.id` and `LoggedItem.plannedWorkoutId` were already in the payload, which is why the bug was a pairing error rather than a missing-information problem.

**Renaming the fields was half the fix, not cosmetic.** The prompt previously read "Weekly running mileage (planned vs actual)", which invited the model to describe the figures as total volume even once they paired correctly — and that framing is precisely how the bug reached the user as "you ran nearly double the planned mileage." `plannedMiles`/`actualMiles` are now `plannedLongRunMiles`/`actualLongRunMiles`, and the rendered snapshot states outright that these are not total weekly volume, that duration-prescribed runs are excluded from both sides, and that adherence should be read from the completion counts instead. A correct number with a misleading label is still a wrong answer.

**Why the existing tests missed it, precisely.** Not because they used unlinked logs — the two mileage tests already linked theirs. The `planned()` fixture helper defaults to `miles: 5`, so **no test had ever contained a planned run without a distance**, and planned and logged always happened to measure the same quantity. The asymmetry only exists when a duration-prescribed run is in the mix, which is every real week. Recorded because "the tests were wrong" is the wrong lesson; the fixtures were simply never asked to represent the real shape of a training week.

**Verified by reintroducing the bug, not by watching the new tests pass.** Reverting the link restriction fails both new regression tests while all 19 pre-existing tests still pass — the same demonstration Checkpoint 17 used, and the concrete evidence for why nothing caught this originally.

**Verified against the running app, on the exact data that produced the false claim.** Before: "you ran nearly double the planned mileage (14-15mi against ~9mi planned)… 60-70% above planned." After, on the same seeded persona: "your 9.1mi and 9mi long runs hit their targets," no overshoot claim, and a genuine concern surfaced instead — 2 of 6 planned runs completed in 14 days. That count was itself checked against Postgres rather than trusted: 2 logged runs, 6 planned, last logged run 2026-07-28. The new output is not just different, it is true.

**The e2e suite still cannot catch this class of bug and was not asked to.** Its coach assertion is deliberately key-agnostic — it checks that the request settles, not what the guidance says — so the regression coverage belongs in `coach.test.ts`, which is network-free and key-free. The local e2e failure seen during this checkpoint is #48, unrelated, and invisible in CI because the CI job runs without an API key.

## Checkpoint 20 — A latency bound for the coach (#48)

**The bound lives at the `CoachCompletion` seam, not inside the Anthropic client.** `getCoachFeedback` races the completion against a deadline and rejects with a `CoachTimeoutError`. Putting it in `createAnthropicCompletion` instead would have placed it on the far side of the seam the tests substitute, so the guarantee would have been untestable without a real key — the exact property that made this bug invisible in the first place. Bounded at 25s: above the ~11s typical case with headroom, under Playwright's 30s default so the suite always settles, and about as long as anyone should wait for a comment on their own training.

**A slow coach reports `timed_out`, not `failed`.** The `failed` copy tells the reader the server log has the reason — true for a bad key or exhausted credit, actively misleading for a request that was merely slow, where the log has no cause in it and the right action is simply to try again. This is the same argument the schema file already makes for keeping `unavailable` and `failed` apart ("collapsing those two into one state actively misleads whoever is debugging"), applied to a third case rather than re-litigated.

**Recorded rather than glossed: the bound is a race, not a cancellation.** It stops *waiting* on the upstream call but does not abort it, so a slow request runs to completion and its result is discarded. Aborting properly would mean threading an `AbortSignal` through `CoachCompletion`, widening the seam every test has to satisfy, for one low-volume endpoint. Deliberately not done, and written down so the gap is a known trade-off rather than an oversight someone rediscovers later.

**The e2e config had been silently capping every test at 30s.** `playwright.config.ts` carried `timeout: 60_000`, but inside the `webServer` block, where it governs server startup only. With no test-level timeout, Playwright's 30s default applied, so the coach spec's `toBeEnabled({ timeout: 60_000 })` could never wait the 60s it declared. An explicit top-level `timeout` now makes the declared tolerance real. Worth naming because the config *looked* correct at a glance — the value was right, the scope was wrong.

**Why four green suite runs are not the proof, and what is.** The suite now passes consistently (4/4 across four consecutive runs, 13-16s each against 36-45s before), but that is partly because upstream latency happens to be low right now — passing while the tail is quiet is weak evidence. The structural argument is what holds: the request is guaranteed to settle within 25s, the test budget is 60s, and the spec asserts *settling* rather than success, so a 35s upstream that previously blew the 30s budget now lands as `timed_out` and the test still passes. The bound itself is proven by three unit tests through the seam — never-resolves, resolves-inside-the-bound, and rejects-on-its-own-terms — plus forcing the constant to 1ms and confirming in the running app that the card leaves "Thinking…", renders the new copy, and re-enables "Ask again". That manual check is also the only coverage of the router's status mapping, since `complete` is built inline from `process.env` and cannot be injected without a refactor that was out of scope.

**CI stays key-free, and no scheduled real-key job was added.** Adding a paid external dependency to a required check trades one flake for another, and the property worth pinning — does the card give up cleanly — is now deterministic through the seam. A scheduled job for real-latency monitoring was considered and declined: nothing is currently asking for it, and it can be added if that changes.

## Checkpoint 21 — A selected zero rendered as nothing (#44)

**Diagnosed before fixing, and the first plausible explanation was wrong.** Radix's `shouldShowPlaceholder(value)` is `value === "" || value === undefined`, so the obvious theory — that `"0"` was being treated as empty — is ruled out by the library's own source; our value is the string `"0"` and never reaches that path. Recording the dead end so nobody re-derives it.

**The real mechanism, established by measuring the DOM rather than by reading more source.** Radix renders the trigger's text by portalling the *selected* item's children into the value node (`ReactDOM.createPortal(itemTextProps.children, context.valueNode)`). Our `SelectItem`s passed a raw number as children, and that portal drops a bare numeric `0` while rendering `"0"` correctly. Probed in the running app: with `{n}` the value span had **0 child nodes and empty `innerHTML`**; with `{String(n)}` it had 1 child node containing "0". Both `textContent` and `innerText` were checked, specifically to rule out a layout fault masquerading as a rendering one — they agreed, so the text genuinely was not in the DOM.

**The option list was never affected, which is why this was easy to miss.** `{0}` renders "0" normally in the list; only the portal path into the trigger loses it. So the bug was invisible while the dropdown was open and appeared only once a zero was actually chosen — and the persisted form value was correct throughout. Nothing was wrong except what the user could see.

**Fixed at the pattern, not the instance.** All three numeric selects on the intake form now render `{String(n)}`, though only `DAY_COUNT_OPTIONS` contains a zero today. `RUNNING_DAY_OPTIONS` and `CUSTOM_LIFT_DAY_OPTIONS` start at 1 and were never broken — they are one config change away from it, and leaving them on the fragile form would make the fix a coincidence rather than a rule. A comment at the constants says why, because `{String(n)}` reads like something to simplify back.

**Why it was worth a checkpoint at all.** 0 bike days is the seeded demo's own configuration and one of only two clean day-economy setups Checkpoint 16 identified — the recommended value was the one that displayed as nothing.

**Guarded in e2e, and the guard was seen to fail.** No component-test layer exists in this project (unit tests cover the pure packages, e2e covers the app), so `e2e/intake-selects.spec.ts` asserts the trigger's *rendered text* rather than the submitted value — the value was always right, which is precisely why nothing caught this. Reintroducing `{n}` fails it with `Expected: "0", Received: ""`.

## Checkpoint 22 — Correcting the strength-logging finding, and specifying the real gap

**The checkpoint's own premise turned out to be false, and finding that out was the work.** CP22 was scoped as a spec for #42 and #43 on the understanding that per-exercise strength logging did not exist. It does, and has since Checkpoint 9: `LogSession.tsx` carries `ExerciseLog`/`SetEntry` state, builds set rows from the prescription's `setsReps`, submits `setLog`, and `setLogEntrySchema` accepts it. Verified in the running app — a Wednesday Glute Gladiator row opens a form with Barbell Hip Thrust at 4x4-6 and four set rows, coaching notes attached, plus "Add set".

**How the persona review got it wrong, precisely.** It opened `/log` directly, selected "Lift" from the type dropdown, saw a plain form, and generalised. That is the freeform path, which has no linked prescription. The "Log this" path was never tried for this question. The review's stated method is to use the product rather than reason about the code, and it did — through one door of two. Recorded because the failure mode is the mirror image of the Checkpoint 17 bug: that one hid because nobody clicked the thing, this one was manufactured by clicking only one thing.

**Corrected in place, in three locations, rather than rewritten.** The review's finding 3, its summary table, and Checkpoint 18's entry above all carried the false claim. Each keeps its original text with the correction beside it, following the Checkpoint 12 precedent — an entry is the record of what was believed at the time. A review that overstates a gap damages trust exactly as much as one that misses it, and this one had already been cited as justification for a checkpoint's scope.

**The real gap, confirmed by observation before being written down: no exercise can be logged unless the plan prescribed it.** Structured mode is gated on `isStructuredLift` (`linkedExercises && linkedExercises.length > 0`), so it is unreachable for Custom-mode sessions — which prescribe nothing by design — and for freeform lift logs. Both checked in the app: 0 set rows in each case. This is one gap seen twice, not two, and it is why #42 and #43 are genuinely coupled even though the originally stated reason for coupling was wrong.

**No schema change is required, which was checked rather than assumed.** `setLogEntrySchema` already expresses `{ exercise, sets: [{ reps, weightLbs }] }` and `SessionLog.setLog` is `Json?`. Two constraints were surfaced by reading it that will bite the implementation: `reps` is a positive integer, so an empty set row must be dropped rather than submitted as zero, and `weightLbs` is non-negative, so 0 is a legitimate bodyweight entry rather than an unfilled one.

**Deliberately not decided here: plan-matching for freeform lift logs.** A user may lift on a planned day and reach the form via `/log`, and the link is set only when arriving from a dashboard row. The spec names this as an absence rather than solving it, so the next reader knows it is a choice.

## Checkpoint 23 — Logging an exercise the plan didn't prescribe (#42, #43)

**One flag was answering three unrelated questions, and separating them was most of the work.** `isStructuredLift` (`linkedExercises && linkedExercises.length > 0`) simultaneously hid the Type field, hid the Distance field, and showed the exercise section. Gating exercises on "is this a lift" without untangling it would also have hidden Type on a freeform lift, where the runner still has to choose it. Now: the exercise section keys off `type === "LIFT"`, Distance off `type === "RUN" || "BIKE"` (which incidentally closes the review's secondary note about Distance appearing on lifts and rest days), and Type is always shown.

**An earlier cut of this checkpoint hid the Type field whenever the runner arrived from a dashboard row, and an existing test caught that it was wrong.** The reasoning seemed sound — the type is already known — but runs previously *showed* Type, and Checkpoint 17's e2e asserts the preselected type as the visible proof that the prescription travelled with the click. Hiding it removed that confirmation and broke the assertion. Reverted to always showing it, which is also simpler and lets a runner correct a mis-clicked row. Recorded because the plan was wrong and the test suite, not review, is what said so.

**Rows carry their own identity and their own prescription.** Cards were keyed by exercise *name* and templates looked up by array *index* — both fine for a fixed list, both broken the moment rows can be added or removed: an added exercise starts nameless and two can collide, and removing a row shifts every later index onto the wrong template. Each row now has a generated id and carries its own `setsReps`/`notes`, absent for user-added exercises.

**Submit semantics were left alone deliberately.** Sets with no reps are dropped, then exercises with no sets are dropped — already the behaviour, and already the spec's chosen answer for "an exercise with no sets", so there was nothing to redesign. Blank weight still means 0, which the spec calls out as a real bodyweight entry rather than an unfilled one; verified in Postgres that a Pull-up persisted as `weightLbs: 0`. The one addition is dropping an added exercise that was never named, so nothing persists as `exercise: ""`.

**Adherence is structurally untouched, which is stronger than spot-checking it.** Spec rule 7 required that removing a prescribed exercise must not imply a missed session. Rather than compare coach output before and after, `grep -c setLog packages/contracts/src/lib/coach.ts` returns **0** — the snapshot never reads exercise detail at all, so no amount of editing `setLog` can move an adherence number. Confirmed alongside it that a log with a prescribed exercise removed still persists with `plannedWorkoutId` set.

**Verified by reintroducing the gate, and the result was the useful part.** Restoring `exerciseLogs.length > 0` fails the Custom-mode and freeform-lift assertions while the prescribed-exercise test *still passes* — a precise demonstration of why the old gate looked correct for years of use. Round-trip confirmed by querying Postgres rather than trusting History's "N exercises logged" string: the prescribed exercise that was filled and the user-added one both persist with the exact reps and weights entered, while the removed and unfilled rows are absent.

**Custom mode's caption (#43) ships with this, not before it.** It now says the app reserves the days, keeps them clear of hard runs, and leaves the lifting to the runner — a claim that was aspirational until this checkpoint made logging possible. `custom.ts` is unchanged; its exercise-free design was never the gap.

## Checkpoint 24 — Second step-back persona review

**Run after Checkpoint 23 rather than after Checkpoint 25, because 23 rebuilt the flow the last review got wrong.** The previous review concluded per-exercise logging did not exist, having tested `/log` with the type dropdown and never opened the "Log this" path. A flow with that history, just restructured, earns a pass before more is built on top of it.

**The method fix was the point, and it is what found the important bug.** This review carried an explicit rule: for any capability, enumerate every route that reaches it and exercise each; a conclusion drawn from one path is provisional until the others are checked. The log form was tested from all three doors and the coach in every state it can occupy. Finding 1 is reachable through exactly one door — a lift row — which the previous review never opened.

**Finding 1 (#56): changing Type on a session opened from a plan row strands the link across types.** Open Thursday's Lift row, switch Type to Run, submit, and Postgres holds a `RUN` log of 3.1 miles linked to a `LIFT` planned workout. The damage is in adherence and it cuts both ways: `loggedPlanIds` is built from `plannedWorkoutId` with no type check (`coach.ts:138-140`), so the planned lift drops out of `missedSessions`, while `strengthCompleted` counts by type (`coach.ts:151`), so the run does not count as strength either. The session is neither missed nor completed — it simply disappears. **Not a Checkpoint 23 regression**: arriving from a run row always showed the Type field, so run→lift was already possible. Checkpoint 23 made Type visible on lift arrivals too, deliberately and for a good reason, which doubled the ways in. Filed rather than fixed because the right answer — refuse the switch, drop the link, or warn — is a product decision.

**A copy bug that Checkpoint 23 made reachable rather than introduced: "1 exercises logged" (#59).** Only the seed wrote `setLog` before, and seeded lifts carry six to eleven exercises, so the singular case could not occur. Letting users log their own made a latent string bug visible. Recorded because it is a good example of a feature exposing existing defects rather than creating them.

**Dead contract surface found (#60): the coach's `no_plan` state is unreachable.** `CoachCard` only renders on the dashboard, and since Checkpoint 17 the dashboard redirects to `/intake` whenever `plan` is null. The schema documents it and the card has copy for it; neither can happen. Harmless, but it is exactly the kind of thing that accumulates unnoticed and later reads as a supported state.

**Six findings, none fixed, consistent with what a review checkpoint is for.** Fixing them would have bundled a product decision about type-switching, two copy fixes, a form-layout question and a dead-state cleanup into one push.

**Re-verified rather than inherited, again.** Both engine-level deferrals from Checkpoint 13 confirmed still open by calling `generatePlan` directly: peak long-run distance is 19 mi for a novice at 8 mi/week *and* for an experienced runner at 40 mi/week, and neither plan has a single cutback week in its first 14. #45, #46 and #47 re-checked in situ and all still valid — #47 confirmed on screen, with `BASE PHASE` in the header and `· Peak block ·` on four rows of the same week.

**What held up is recorded alongside what did not**, as at Checkpoint 18: the rebuilt log form works from all three doors, Custom mode now delivers what its caption promises, Checkpoint 20's `timed_out` copy reads well in situ, and Checkpoints 17, 19 and 21 all still hold.

## Checkpoint 25 — A logged session can't claim a planned workout it doesn't match (#56)

**The link is validated on the server, because it is a claim about adherence rather than a UI convenience.** `logSession` wrote `plannedWorkoutId` verbatim, with no type check and — found while planning — no ownership check either, so nothing stopped a log attaching to another user's planned session. Both are now checked in one lookup, since fixing the type mismatch requires fetching that row anyway.

**The two failure modes are handled differently on purpose, and the asymmetry is the decision.** A workout that isn't the caller's is rejected outright: there is no correct interpretation of a link to someone else's data. A type mismatch drops the link and saves the session as freeform, because there *is* a correct interpretation — the runner did something other than what was planned, so it stands alone and the planned session stays legitimately unlogged. Rejecting that would lose a real entry over a recoverable mismatch. The drop is `console.warn`ed rather than swallowed, matching how the coach reports its own degraded paths, so a client that starts sending mismatches stays visible.

**Type stays editable, and the client says what will happen rather than doing it silently.** Checkpoint 23 made the Type field visible on plan arrivals deliberately — it is the proof the prescription travelled with the click — and detaching is exactly the escape hatch a mis-clicked row needs. Switching away now shows an inline note naming the session being left behind ("This no longer matches Lower A: Glute + Hinge Strength, so it'll be logged on its own — that planned session will still show as not done"), and switching back re-attaches, since the note describes current state rather than a one-way door.

**Verified against the exact query that found the bug.** Checkpoint 24 produced a `RUN` log of 3.1 miles joined to a `LIFT` planned workout; the same join now returns **0 rows**, while the run itself persists unlinked with its distance intact — the entry was preserved, only the false claim removed. Thursday's Lower A shows **0 logs claiming it**, which is the true statement the bug was suppressing: the runner skipped it, and adherence can now say so.

**Guards seen to fail before being trusted.** Neutralising the validation fails both new integration tests — the mismatch case and the ownership case — while "keeps the link when the types match" still passes, which is the shape that shows the guard is specific rather than blanket. Integration tests were the right layer: the invariant is about what reaches Postgres, and `router.integration.test.ts` runs against a real one.

**Not a Checkpoint 23 regression, recorded so the history stays straight.** Arriving from a run row always exposed the Type field, so run→lift was possible long before. Checkpoint 23 made Type visible on lift arrivals too, which doubled the ways in and is how the second persona review found it.

## Checkpoint 26 — Proving a migration survives existing data

**The gap was demonstrated before it was closed, and the demonstration is the argument.** The same migration — `ALTER TABLE "SessionLog" ADD COLUMN ... TEXT NOT NULL` with no default — applies cleanly to an empty database and fails on a populated one with `23502: contains null values`. CI only ever migrated an empty database, so it would have gone green; migrations run at server *start*, so the failure would have surfaced as a production boot failure. Recorded with the actual error code because "migrations might break on real data" is a vague worry and this is a specific, reproducible one.

**A CI job, not a checklist item, because Checkpoint 18's lesson was that discipline dependencies fail.** `migration-over-data` migrates to the base branch's schema, inserts a fixture, restores the branch's migrations and applies them over populated tables. The roadmap originally proposed rehearsing against a production dump before schema work — a manual ritual, and rituals only break when someone forgets, which is the normal case. The manual path still exists for the question automation cannot answer, but it is no longer the guard.

**The fixture is raw SQL rather than `db:seed`, and that is not laziness.** At the moment it runs, the database is at the *base* schema while the checked-out Prisma client is the *pull request's*. Seeding through that client would be asking a new client to write old-schema rows — exactly the coupling the job exists to test, used as the test's own setup. Raw SQL touching only columns stable since the init migration has no such dependency. It inserts one linked and one freeform `SessionLog` specifically, since the link is what adherence is computed from and a migration that broke it would otherwise pass unnoticed.

**Verified in all three directions rather than just the failing one.** Bad migration over data fails (the guard works); the *same* bad migration against an empty database passes (the gap is real, and the existing jobs would have missed it); the corrected migration over the same data passes (the guard is specific, not blanket). A guard that only ever goes red is not obviously distinguishable from one that is broken.

**`db:rehearse` never migrates its source and has no default source.** It dumps, restores into a scratch database, and migrates the copy. A default would eventually be production, so there isn't one. `pg_dump`/`psql` run inside the compose Postgres container because the host has no Postgres client — the container can reach a remote host, so a Railway URL works through the same path. Written down because it looks like an odd choice otherwise. Exercised against the local seeded database: 567 planned workouts and 50 session logs copied and migrated with counts intact, source untouched.

**The job's own first run failed, and the cause was the assertion rather than the fixture.** `SELECT CASE WHEN ... THEN 'ok' ELSE (SELECT 1/0)::text END` — the standard shell trick for turning a false SQL condition into a non-zero exit — reported division by zero even though every condition held. `(SELECT 1/0)` becomes an InitPlan, which Postgres evaluates independently of which CASE branch is taken. A trivial `WHEN 1=1` version of the same expression *does* work, because the planner constant-folds the whole thing away, which is precisely what made it a misleading thing to test the idea against. Replaced with a PL/pgSQL block that `RAISE EXCEPTION`s and reports the actual counts, so a future failure names what broke instead of saying "division by zero". Recorded because the first instinct — that the fixture had genuinely been destroyed — was wrong, and the CI log alone would have supported it.

**`IS DISTINCT FROM`, not `<>`, on the link check.** A migration that nulled `plannedWorkoutId` would make `<>` evaluate to NULL rather than true, and the assertion would pass through the exact failure it exists to catch.

**Deliberately not made a required check yet.** Adding a fourth required status check changes merge behaviour for the repository, which is a settings decision rather than a code one. Worth doing once the job has a few green runs behind it; raised rather than assumed.

**This also settles the Railway pre-prod question, for now.** The specific thing a second environment would have bought — somewhere to find out whether a migration survives real data before production does — is now covered for free. What remains is a shareable staging URL for someone else to test against, which is not a need that exists yet.

## Checkpoint 27 — Editing and deleting a logged session (#10, session half)

**Split #10 rather than closing it.** The session half — fix a typo, remove a duplicate — had no semantics to settle. The plan half does: `SessionLog.plannedWorkoutId` is `onDelete: SetNull` by Checkpoint 14's deliberate choice, so deleting a plan silently unlinks its history and makes that period's adherence unrecoverable, which is defensible but wants deciding rather than inheriting. It also overlaps #15's multi-plan UI, and designing them apart risks two plan-management surfaces. Filed as #64.

**The Checkpoint 25 invariant is shared, not duplicated, because `updateSessionLog` is a second write path to the field #56 was about.** `resolvePlannedWorkoutLink` is now called by both `logSession` and `updateSessionLog`; a copied rule is precisely how #56 would return, through a door no persona review has reason to open. The integration test asserts the detach behaviour *through the update path* rather than trusting that the shared call is reached — verified by bypassing the helper in `updateSessionLog`, which fails that one test while every ownership and field-edit test still passes.

**Hard delete, and no soft-delete column.** Agreed before planning. A `deletedAt` would need filtering in the dashboard, history, progress and coach-snapshot queries, and one forgotten filter silently resurrects a deleted row — a failure mode with no loud symptom. The inline two-step confirm is the safeguard instead.

**Editing reuses the log form rather than adding an inline editor.** That form already handles every field including the exercise rows and Checkpoint 25's detach note; an inline editor in History would mean rebuilding most of it, and two surfaces would drift. History navigates to `/log` with the row's values in router state — the same mechanism "Log this" already uses — and `getSessionHistory` already returns every field needed, so there is no second fetch.

**Update writes nulls explicitly rather than passing `undefined`.** Prisma treats `undefined` as "leave unchanged", so an edit that clears a distance or a note would silently keep the old value. `distanceMiles`, `notes` and `plannedWorkoutId` are written as `null` when absent, and `setLog` as `Prisma.DbNull`. That last one required changing `Prisma` from a type-only to a value import in `router.ts` — server-side only, and the Checkpoint 4 bundle grep was re-run to confirm nothing leaked: `PrismaClient`, `prisma`, `@prisma/client`, `@clerk/express`, `getOrCreateUser` all still zero in the built web bundle.

**Verified that the numbers move with the edit, not just that the row changed.** Logging a 2.0mi run and editing it to 12mi moved the dashboard's weekly total from 7.2 to 17.2 — a delta of exactly 10. A stale aggregate would have been the quiet failure here, since every dashboard, progress and coach figure is derived from these rows.

**An e2e assertion failed on leftover data before it failed on a bug.** The first version asserted a `3.7mi · 37min` row was gone after editing, and a row with those exact values survived — from an earlier failed run of the same spec, not from a defect. Fixed by deriving distinctive durations from `Date.now()`, the pattern `golden-path.spec.ts` already uses. Worth recording because the first reading of that failure was "the edit didn't persist", which the database disproved.

**A second e2e assumption, caught by CI rather than locally: the helper asserted `/dashboard` after submitting.** That holds against the seeded local database and fails in CI, which runs migrated but unseeded — with no plan, Checkpoint 17 routes straight on to `/intake`. Now it waits only for the form to be left behind, since what matters is that the submit went through and the history row proves that. Reproduced the CI condition locally by clearing the plans before re-running, rather than trusting a green local suite that never exercised the empty-database path. Twice in one checkpoint an e2e test encoded an assumption about state rather than about behaviour; both were mine, and both were invisible until something else supplied the counter-example.

**Needed no migration, so Checkpoint 26's guard passed trivially here.** Said plainly rather than implied: the guard was still right to build — #15 will almost certainly need a column — but it was not what stood between here and #10.
