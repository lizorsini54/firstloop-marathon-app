services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: firstloop
      POSTGRES_PASSWORD: firstloop
      POSTGRES_DB: firstloop
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U firstloop"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres-data:
